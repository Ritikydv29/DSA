class Solution {
public:
    void reverseString(vector<char>& s) {
        int i=0;
        int j=s.size()-1;
        vector<char>ar(1);
        while(i<j)
        {
            ar[0]=s[j];
            s[j]=s[i];
            s[i]=ar[0];

            i++;
            j--;
        }
    }   
}; 
